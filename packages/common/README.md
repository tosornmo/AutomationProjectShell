# packages/common

Shared types, OpenAPI specs, and utilities for the monorepo.

Ideas:
- `src/types/` — TypeScript interfaces and shared DTOs
- `openapi/` — OpenAPI contracts
- `utils/` — cross-language helpers
