# Backend API tests

This folder will contain API tests for the backend services.

Structure ideas:
- `tests/` — API test suites (e.g., using Jest + Supertest, or Pytest + requests)
- `fixtures/` — test fixtures and test data
- `ci/` — CI workflows for running API tests

Preferred stack for API tests? (Node: Jest+Supertest, Python: Pytest, other)