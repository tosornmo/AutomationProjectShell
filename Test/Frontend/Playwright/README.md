# Playwright tests (web)

This folder will contain Playwright tests for web frontend.

Structure ideas:
- `tests/` — test specs
- `playwright.config.{js,ts}` — configuration
- `ci/` — CI workflows for running Playwright tests

Would you like TypeScript for Playwright tests?