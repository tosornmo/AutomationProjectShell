{
  "name": "automationprojectshell",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "packages/*",
    "tests/*"
  ],
  "scripts": {
    "build": "pnpm -w -r run build",
    "test": "pnpm -w -r run test",
    "lint": "pnpm -w -r run lint"
  }
}
